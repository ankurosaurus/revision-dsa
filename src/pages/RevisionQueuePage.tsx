import React from 'react';
import { RevisionQueue } from '../components/revision/RevisionQueue';

export const RevisionQueuePage: React.FC = () => {
  return (
    <div className="max-w-5xl mx-auto py-2">
      <RevisionQueue />
    </div>
  );
};
